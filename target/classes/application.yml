spring:
  datasource:
    username: root
    password: Newuser@123
    url: jdbc:mysql://localhost:3306/javatech
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 9191
